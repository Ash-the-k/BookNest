<!DOCTYPE html>
<html>
<head>
  <title>All Books</title>
</head>
<body>
  <h1>All Books</h1>

  <% if (books.length === 0) { %>
    <p>No books found.</p>
  <% } else { %>
    <ul>
        <% books.forEach(book => { %>
            <li>
            <a href="/books/<%= book.id %>">
                <%= book.title %> — <%= book.author %>
            </a>
            </li>
        <% }) %>
    </ul>
  <% } %>
</body>
</html>
